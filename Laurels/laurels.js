// var toTitleCase = document.querySelectorAll(".toTitle")

// toTitleCase.forEach(element => {
//     element.innerText = element.innerText.toLowerCase().replace(/\b(\w)/g, x => x.toUpperCase());
// });